{
  "$schema": "uxflows://capability-mock/v0",
  "capability_id": "cap_place_order",
  "variant": "success",
  "description": "Kitchen accepts the order. cap_place_order has no declared outputs, so the returns object is annotation-only — the spec doesn't bind it to any variable.",
  "behavior": {
    "kind": "static",
    "returns": { "ack": "ok", "ticket_id": "T-1042" }
  }
}
